# Database
DATABASE_URL=postgresql://user:password@localhost:5433/mydb

# Auth
JWT_SECRET=change-me-to-a-random-string

# RSI Hub Integration (injected automatically for RSI workers)
RSI_HUB_URL=https://rsi.digify.no/api
RSI_HUB_TOKEN=

# Server
PORT=4001
HOST=127.0.0.1
NODE_ENV=development

# Optional: disable dev-login endpoint
# DISABLE_DEV_LOGIN=true
